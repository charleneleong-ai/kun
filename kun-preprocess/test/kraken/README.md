- Kraken [docs](http://kraken.re/)

```bash
$ pip install kraken
$ kraken -i image.tif bw.tif binarize
$ kraken -i bw.tif lines.json segment -d vertical-rl -b -w --maxcolseps 99
$ python kraken_crop.py
```

